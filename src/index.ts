/**
 * alpha-shadow — multi-tenant POS engine.
 *
 * Phase 0: project scaffold only. There is intentionally no runtime behaviour
 * here yet; the composition root (config validation that fails closed on any
 * missing secret, DI wiring, startup guards) arrives with Phases 1–3.
 */
export const PROJECT_NAME = 'alpha-shadow' as const;
