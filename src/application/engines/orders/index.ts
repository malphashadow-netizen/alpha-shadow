// Phase 6 adds ONLY the atomic order/tax integration port. Order lifecycle,
// pricing, persistence schema, payments and other order permissions are future.
export { OrderTaxCoordinator } from './order-tax-coordinator.ts';
